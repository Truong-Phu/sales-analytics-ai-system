# Requires Python 3.13+
# Cài đặt: pip install -r requirements.txt
# Môi trường khuyến nghị: python -m venv .venv && .venv\Scripts\activate (Windows)

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
prophet>=1.1.5
pandas>=2.2.0
scikit-learn>=1.5.0
numpy>=2.0.0
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
cryptography>=42.0.0
python-dotenv>=1.0.0
joblib>=1.4.2
httpx>=0.27.0
pydantic>=2.7.1
anthropic>=0.25.0

# Chức năng mới – Tháng 5/2026
mlxtend>=0.23.0          # Market Basket Analysis (Apriori algorithm)
apscheduler>=3.10.4      # Job scheduler (KPI email hàng ngày)
jinja2>=3.1.4            # Template engine cho email KPI
