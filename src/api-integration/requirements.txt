# ── HTTP & API ────────────────────────────────────────────────────────────────
requests==2.31.0          # HTTP client cho tất cả API connectors
urllib3==2.1.0            # Dependency của requests
beautifulsoup4==4.12.3    # HTML parser dùng bởi google_scraper.py
lxml==5.2.1               # HTML parser backend cho BeautifulSoup (nhanh hơn html.parser)

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9    # PostgreSQL adapter (binary = không cần build từ source)

# ── Xử lý dữ liệu ────────────────────────────────────────────────────────────
pandas==2.2.1             # Đọc Excel/CSV, xử lý DataFrame
openpyxl==3.1.2           # Đọc file .xlsx (dependency của pandas)
xlrd==2.0.1               # Đọc file .xls cũ

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.0.1      # Đọc biến môi trường từ file .env

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.1.1             # Test framework
pytest-cov==5.0.0         # Coverage report
pytest-mock==3.14.0       # Mock helpers
pytest-asyncio==0.23.6    # Async test support

# ── Mã hóa token (AES-256-GCM cho integrations.access_token) ──────────────────
cryptography==42.0.5      # AES-256-GCM encrypt/decrypt cho integration tokens

# ── Scheduler (đồng bộ tự động theo interval) ────────────────────────────────
APScheduler==3.10.4       # Lên lịch sync jobs theo interval/cron

# ── Code quality ──────────────────────────────────────────────────────────────
black==24.3.0             # Code formatter
flake8==7.0.0             # Linter
mypy==1.9.0               # Type checker
